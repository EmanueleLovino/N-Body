#pragma once

/// constants used across the whole simulation
namespace nbody::constants {

inline constexpr float G = 6.674e-11f;
inline constexpr float soft = 0.035f;
}  // namespace nbody::constants
